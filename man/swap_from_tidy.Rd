% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidymodels.R
\name{swap_from_tidy}
\alias{swap_from_tidy}
\title{Convert a Data Frame to a switchbox Matrix}
\usage{
swap_from_tidy(data, outcome = NULL)
}
\arguments{
\item{data}{A data frame, tibble, or matrix with samples in rows and
numeric features in columns.}

\item{outcome}{Optional; a character string naming the column that contains
the class labels. If provided, that column is excluded from the matrix and
returned separately as a factor.}
}
\value{
If \code{outcome} is \code{NULL}, a numeric matrix with features in rows and
samples in columns. If \code{outcome} is provided, a list with components:
\itemize{
\item \code{x}: the numeric matrix (features x samples)
\item \code{y}: a factor of class labels
}
}
\description{
Converts a data frame or tibble with samples in rows and features in
columns (the tidymodels convention) to a numeric matrix with features in
rows and samples in columns (the switchbox convention).
}
\examples{
df <- data.frame(
  class = factor(c("a", "b", "a", "b")),
  gene1 = c(1.2, 3.4, 1.5, 3.1),
  gene2 = c(5.6, 2.1, 5.9, 2.3)
)
result <- swap_from_tidy(df, outcome = "class")
result$x
result$y
}
