% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidymodels.R
\name{swap_to_tidy}
\alias{swap_to_tidy}
\title{Convert a switchbox Matrix to a Tibble}
\usage{
swap_to_tidy(x, y = NULL, outcome = "class")
}
\arguments{
\item{x}{A numeric matrix with features in rows and samples in columns.}

\item{y}{Optional; a factor or vector of class labels. If provided, it is
added as a column named by \code{outcome}.}

\item{outcome}{Character; column name for the class labels. Default is
\code{"class"}.}
}
\value{
A data frame with samples in rows. If \code{y} is provided, it includes
a factor column for the outcome.
}
\description{
Converts a numeric matrix with features in rows and samples in columns
(the switchbox convention) to a data frame with samples in rows and
features in columns (the tidymodels convention).
}
\examples{
data(trainingData)
tidy_df <- swap_to_tidy(matTraining, trainingGroup, outcome = "prognosis")
head(tidy_df[, 1:5])
}
