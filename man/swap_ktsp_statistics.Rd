% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{swap_ktsp_statistics}
\alias{swap_ktsp_statistics}
\title{Compute KTSP Statistics (Votes)}
\usage{
swap_ktsp_statistics(x, classifier, combine_fn)
}
\arguments{
\item{x}{A numeric matrix (features x samples). Must contain the features
referenced in \code{classifier$TSPs}.}

\item{classifier}{A KTSP classifier as returned by \code{\link[=swap_train_ktsp]{swap_train_ktsp()}} or
\code{\link[=swap_train_1tsp]{swap_train_1tsp()}}.}

\item{combine_fn}{An optional function to aggregate the per-TSP votes. If
not provided, uses the default signed voting statistic.}
}
\value{
A list with:
\itemize{
\item \code{statistics}: named numeric vector of aggregate per-sample statistics
\item \code{comparisons}: matrix (samples x pairs) of individual TSP comparisons
}
}
\description{
Computes the per-sample KTSP voting statistics by comparing the feature
values within each top-scoring pair. The default statistic is the sum
of votes minus K/2 (majority-wins principle).
}
\examples{
data(trainingData)
classifier <- swap_train_ktsp(matTraining, trainingGroup, k_range = 3:8)
stats <- swap_ktsp_statistics(matTraining, classifier)
head(stats$statistics)
}
