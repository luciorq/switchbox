% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{swap_ktsp_cv}
\alias{swap_ktsp_cv}
\title{K-Fold Cross-Validation for KTSP}
\usage{
swap_ktsp_cv(x, y, classes = NULL, k = 4, folds = NULL, randomize = TRUE, ...)
}
\arguments{
\item{x}{Numeric matrix (features x samples).}

\item{y}{Factor with two levels.}

\item{classes}{Character vector: \code{c(case, control)}. If \code{NULL}, derived
from \code{levels(y)}.}

\item{k}{Integer; number of folds. Default is \code{4}.}

\item{folds}{An optional pre-specified list of index vectors (one per fold).
If provided, \code{k} is ignored.}

\item{randomize}{Logical; randomly permute samples before splitting?
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=swap_train_ktsp]{swap_train_ktsp()}}.}
}
\value{
A list with:
\itemize{
\item \code{cv}: list of per-fold results
\item \code{folds}: the fold index lists used
\item \code{predictions}: combined predictions across all folds
\item \code{decision_values}: combined decision values
\item \code{stats}: overall prediction statistics
\item \code{truth}: the true labels in fold order
\item \code{randomized_indices}: permutation indices used (if randomized)
}
}
\description{
Performs K-fold cross-validation by training a KTSP classifier on K-1 folds
and evaluating on the held-out fold, then combining results.
}
\examples{
data(trainingData)
set.seed(42)
cv_result <- swap_ktsp_cv(matTraining, trainingGroup, k = 3)
cv_result$stats
}
