% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{swap_calculate_scores}
\alias{swap_calculate_scores}
\title{Calculate Pairwise TSP Scores}
\usage{
swap_calculate_scores(
  x,
  y,
  classes = NULL,
  filter_fn = swap_filter_wilcoxon,
  restricted_pairs = NULL,
  handle_ties = FALSE,
  verbose = FALSE,
  score_fn = swap_calculate_signed_tsp_scores,
  score_opts = list(),
  ...
)
}
\arguments{
\item{x}{A numeric matrix with features (e.g. genes) in rows and samples in
columns. Must have rownames.}

\item{y}{A factor with exactly two levels indicating the class of each
sample.}

\item{classes}{Character vector of length 2 specifying \code{c(case, control)}.
If \code{NULL}, derived from \code{levels(y)}.}

\item{filter_fn}{A function for feature filtering, or \code{NULL} to use all
features. The function should accept \verb{(y, x, ...)} and return a character
vector of feature names. Default is \code{\link[=swap_filter_wilcoxon]{swap_filter_wilcoxon()}}.}

\item{restricted_pairs}{An optional two-column character matrix specifying
candidate feature pairs to evaluate. Both columns must contain feature
names present in \code{rownames(x)}.}

\item{handle_ties}{Logical; if \code{TRUE}, uses an extended scoring method that
explicitly handles tied feature values.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages.}

\item{score_fn}{A function that computes pairwise scores. Must have the
signature \verb{(y, x1, x2, classes, restricted_pairs, handle_ties, verbose, score_opts)}. Default is \code{\link[=swap_calculate_signed_tsp_scores]{swap_calculate_signed_tsp_scores()}}.}

\item{score_opts}{A list of additional options passed to \code{score_fn}.}

\item{...}{Additional arguments passed to \code{filter_fn}.}
}
\value{
A list with components:
\itemize{
\item \code{score}: named numeric vector of pair scores (names are \code{"gene1,gene2"})
\item \code{labels}: character vector of length 2 with class labels
\item \code{tie_vote}: integer vector of tie-vote types (0 = both, 1 = class 0
favoured, 2 = class 1 favoured)
\item \code{signed}: logical indicating whether scores are signed
}
}
\description{
Computes pairwise Top-Scoring Pair (TSP) scores for all feature pairs or a
restricted set of pairs. This is the main entry point for score computation.
}
\examples{
data(trainingData)
scores <- swap_calculate_scores(matTraining, trainingGroup)
head(scores$score)
}
