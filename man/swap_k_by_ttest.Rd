% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{swap_k_by_ttest}
\alias{swap_k_by_ttest}
\title{Select K by T-Test}
\usage{
swap_k_by_ttest(x, y, score_table, classes, k_range, k_opts = list())
}
\arguments{
\item{x}{Numeric matrix (features x samples).}

\item{y}{Factor with two levels.}

\item{score_table}{Data frame of scored pairs (from \code{\link[=swap_make_tsp_table]{swap_make_tsp_table()}}).}

\item{classes}{Character vector of length 2: \code{c(case, control)}.}

\item{k_range}{Integer vector of candidate K values.}

\item{k_opts}{List of additional options (currently unused).}
}
\value{
Integer vector of selected row indices into \code{score_table}.
}
\description{
Selects the optimal number of top-scoring pairs (K) by maximizing the
t-test statistic between the KTSP vote distributions of the two classes.
}
\examples{
data(trainingData)
scores <- swap_calculate_scores(matTraining, trainingGroup)
tbl <- swap_make_tsp_table(scores, maxk = 10)
sel <- swap_k_by_ttest(
  matTraining, trainingGroup, tbl,
  rev(levels(trainingGroup)), 2:8, list()
)
}
