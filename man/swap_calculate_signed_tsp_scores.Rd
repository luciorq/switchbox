% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{swap_calculate_signed_tsp_scores}
\alias{swap_calculate_signed_tsp_scores}
\title{Calculate Signed TSP Scores}
\usage{
swap_calculate_signed_tsp_scores(
  y,
  x1,
  x2 = NULL,
  classes = NULL,
  restricted_pairs = NULL,
  handle_ties = FALSE,
  verbose = FALSE,
  score_opts = list()
)
}
\arguments{
\item{y}{A factor with exactly two levels.}

\item{x1}{A numeric matrix (features x samples).}

\item{x2}{A numeric matrix (features x samples). Defaults to \code{x1}.}

\item{classes}{Character vector of length 2: \code{c(case, control)}.}

\item{restricted_pairs}{Optional two-column character matrix.}

\item{handle_ties}{Logical; use tie-handling scoring method?}

\item{verbose}{Logical; print progress messages?}

\item{score_opts}{Additional options (unused, for API compatibility).}
}
\value{
A list with \code{score}, \code{labels}, \code{tie_vote}, and \code{signed} components.
}
\description{
Computes the signed Top-Scoring Pair scores using an efficient C
implementation. The signed score for a pair (i, j) is:
\code{P(Xi < Xj | class1) - P(Xi < Xj | class0) - P(Xi > Xj | class1) + P(Xi > Xj | class0)}.
}
\examples{
data(trainingData)
scores <- swap_calculate_signed_tsp_scores(
  trainingGroup, matTraining
)
head(scores$score)
}
