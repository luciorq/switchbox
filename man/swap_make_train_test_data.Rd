% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{swap_make_train_test_data}
\alias{swap_make_train_test_data}
\title{Create Random Train/Test Split}
\usage{
swap_make_train_test_data(x, y, classes = NULL, p = 0.5)
}
\arguments{
\item{x}{Numeric matrix (features x samples).}

\item{y}{Factor of class labels.}

\item{classes}{Character vector: \code{c(case, control)}. If \code{NULL}, derived
from \code{levels(y)}.}

\item{p}{Numeric; proportion of samples for training. Default is \code{0.5}.}
}
\value{
A list with components:
\itemize{
\item \code{train_x}, \code{test_x}: data matrices
\item \code{train_y}, \code{test_y}: label factors
\item \code{train_ids}, \code{test_ids}: sample indices
\item \code{classes}: the class labels used
}
}
\description{
Randomly splits a dataset into training and testing sets while maintaining
class proportions.
}
\examples{
data(trainingData)
set.seed(42)
split <- swap_make_train_test_data(matTraining, trainingGroup)
dim(split$train_x)
dim(split$test_x)
}
