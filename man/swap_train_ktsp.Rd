% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{swap_train_ktsp}
\alias{swap_train_ktsp}
\title{Train a K-Top-Scoring-Pair (KTSP) Classifier}
\usage{
swap_train_ktsp(
  x,
  y,
  classes = NULL,
  k_range = 2:10,
  filter_fn = swap_filter_wilcoxon,
  restricted_pairs = NULL,
  handle_ties = FALSE,
  disjoint = TRUE,
  k_selection_fn = swap_k_by_ttest,
  k_opts = list(),
  score_fn = swap_calculate_signed_tsp_scores,
  score_opts = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix with features in rows and samples in columns.
Must have rownames.}

\item{y}{A factor with exactly two levels indicating the class of each
sample.}

\item{classes}{Character vector of length 2: \code{c(case, control)}. If \code{NULL},
derived from \code{levels(y)}.}

\item{k_range}{Integer vector specifying the range of K values to consider.
Default is \code{2:10}.}

\item{filter_fn}{Feature filtering function. Default is
\code{\link[=swap_filter_wilcoxon]{swap_filter_wilcoxon()}}. Set to \code{NULL} to use all features.}

\item{restricted_pairs}{An optional two-column character matrix specifying
candidate feature pairs.}

\item{handle_ties}{Logical; use tie-handling scoring? Default is \code{FALSE}.}

\item{disjoint}{Logical; require feature pairs to be disjoint? Default is
\code{TRUE}.}

\item{k_selection_fn}{Function to select K. Default is \code{\link[=swap_k_by_ttest]{swap_k_by_ttest()}}.
Alternatives include \code{\link[=swap_k_by_measurement]{swap_k_by_measurement()}}.}

\item{k_opts}{List of additional options passed to \code{k_selection_fn}.}

\item{score_fn}{Scoring function. Default is
\code{\link[=swap_calculate_signed_tsp_scores]{swap_calculate_signed_tsp_scores()}}.}

\item{score_opts}{Additional options passed to \code{score_fn}.}

\item{verbose}{Logical; print progress messages?}

\item{...}{Additional arguments passed to \code{filter_fn}.}
}
\value{
A list (the classifier) with components:
\itemize{
\item \code{name}: character string like \code{"5TSPS"}
\item \code{TSPs}: character matrix with K rows and 2 columns (gene1, gene2)
\item \code{score}: numeric vector of pair scores
\item \code{tie_vote}: factor indicating tie handling per pair
\item \code{labels}: character vector of class labels
}
}
\description{
Trains a binary classifier based on K disjoint top-scoring pairs of
features. The number of pairs K is selected automatically from \code{k_range}
using a selection method (by default, a t-test-based criterion).
}
\examples{
data(trainingData)
classifier <- swap_train_ktsp(matTraining, trainingGroup, k_range = 3:8)
classifier$TSPs
}
